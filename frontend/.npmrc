# npm 客户端健壮性（Phase 6 §13）。不绑定特定 registry，保持可迁移。
# 如需在特定网络下加速/换源，可在 CI 或本地覆盖：
#   npm config set registry https://registry.npmmirror.com
fetch-retries=5
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=120000
fetch-timeout=600000
fund=false
audit=false
